'use strict';

exports.__esModule = true;

var _Rect = require('../geom/Rect');

var _Rect2 = _interopRequireDefault(_Rect);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var resolveColumns = function resolveColumns(container) {
  var bbox = container.bbox,
      columns = container.columns,
      columnGap = container.columnGap;

  var columnWidth = (bbox.width - columnGap * (columns - 1)) / columns;

  var x = bbox.minX;
  var result = [];

  for (var index = 0; index < columns; index++) {
    result.push(new _Rect2.default(x, bbox.minY, columnWidth, bbox.height));
    x += columnWidth + container.columnGap;
  }

  return result;
};

exports.default = resolveColumns;